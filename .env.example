# Copy this file to .env.local and fill in values.

# Supabase project URL (HTTPS, not Postgres!) e.g., https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_URL=

# Supabase service role key (server-side only). Keep secret.
SUPABASE_SERVICE_ROLE_KEY=

# Drizzle direct Postgres connection string for migrations/tests
# Example (URL-encode special characters in password; include sslmode=require for Supabase):
# DRIZZLE_DB_URL=postgresql://postgres:ENCODED_PASSWORD@db.xxxxx.supabase.co:5432/postgres?sslmode=require
DRIZZLE_DB_URL=

# Optional: Google Apps Script web app endpoint for Sheets logging
# If omitted, the app will skip forwarding to Sheets.
SHEETS_WEB_APP_URL=
